---
# meta/main.yml

galaxy_info:
    role_name: graylog-collector
    author: mklettner
    description: Install Graylog Collector Sidecar, Filebeat & NXLog latest version or specific version via internet or local package. Also manages graylog-collector settings.
    license: MIT
    min_ansible_version: 2.4

    platforms:

        - name: EL
          versions:
              - 7
              - 6

        - name: Debian
          versions:
              - wheezy
              - stretch

    galaxy_tags:
        - graylog
        - collector
        - logging
        - sidecar
        - nxlog
        - filebeat

dependencies: []
